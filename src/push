#!/bin/bash

shopt -s extglob
cd ../
rm -rv !(src)
cd src
hugo
mv public/* ../
git add -A :/
git commit -m "write"
git fetch origin
git rebase origin/master

if [ "$1" = "on" ]; then
  git push origin master
fi
